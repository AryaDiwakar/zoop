import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Zoop Multimedia Academy ERP",
    template: "%s | Zoop ERP",
  },
  description:
    "Centralized academy ERP for Zoop Multimedia — leads, admissions, curriculum, attendance, projects, portfolio, finance, certificates and reports.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full">{children}</body>
    </html>
  );
}
